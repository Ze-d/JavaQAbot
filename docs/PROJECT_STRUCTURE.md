# Java文档问答系统 - 项目结构说明

## 📁 项目结构

```
JavaQAbot/
├── src/                          # 核心代码目录
│   ├── main/                     # 主程序
│   │   ├── __init__.py
│   │   ├── app.py               # Gradio Web应用入口
│   │   └── main.py              # 启动脚本
│   │
│   ├── core/                     # 核心逻辑
│   │   ├── __init__.py
│   │   ├── agent.py             # Agent智能体
│   │   ├── service.py           # 服务层
│   │   └── config.py            # 配置管理
│   │
│   ├── utils/                    # 工具模块
│   │   ├── __init__.py
│   │   ├── utils.py             # 通用工具（含Brotli错误修复）
│   │   ├── logger_config.py     # 日志配置
│   │   └── data_process.py      # 数据处理工具
│   │
│   ├── prompts/                  # 提示词模块
│   │   ├── __init__.py
│   │   └── prompt.py            # 提示词模板
│   │
│   └── data/                     # 数据模块
│       ├── __init__.py
│       └── data.py              # 数据处理
│
├── test/                         # 测试代码目录
│   ├── unit/                     # 单元测试
│   │   ├── __init__.py
│   │   ├── test_agent.py        # Agent测试
│   │   ├── test_generator.py    # 生成器测试
│   │   ├── test_logger.py       # 日志测试
│   │   └── test_neo4j_config.py # Neo4j配置测试
│   │
│   └── integration/              # 集成测试
│       ├── __init__.py
│       └── evaluate.py          # 系统评估
│
├── scripts/                      # 脚本目录
│   ├── __init__.py
│   ├── fix_brotli_error.py      # Brotli错误修复脚本
│   ├── utils_fixed.py           # 工具修复脚本
│   └── generator.py             # 生成器脚本
│
├── resources/                    # 资源目录
│   ├── data/                    # 数据存储
│   │   ├── db/                  # 向量数据库
│   │   └── inputs/              # 输入数据
│   ├── logs/                    # 日志文件
│   └── .gradio/                 # Gradio配置
│
├── docs/                         # 文档目录
│   ├── README.md
│   ├── TODO.md
│   ├── DEBUG_LOG_GUIDE.md
│   ├── PROJECT_STRUCTURE.md
│   └── API.md                   # API文档（待创建）
│
├── .CLAUDE/                      # 项目配置
├── .git/                         # Git配置
├── .gitignore
├── requirements.txt              # 依赖文件
└── setup.py                      # 安装脚本
```

## 🚀 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 启动应用

```bash
# 方式1：使用启动脚本
python src/main/main.py

# 方式2：直接运行app.py
python src/main/app.py
```

### 3. 运行测试

```bash
# 运行所有测试
pytest test/

# 运行单元测试
pytest test/unit/

# 运行集成测试
pytest test/integration/

# 运行测试并生成覆盖率报告
pytest test/ --cov=src/
```

## 📝 模块说明

### src/main/
**主程序模块**
- `app.py`: Gradio Web界面，提供用户交互界面
- `main.py`: 启动脚本，统一应用启动入口

### src/core/
**核心业务逻辑**
- `agent.py`: Agent智能体，协调多工具问答
- `service.py`: 服务层，封装业务逻辑
- `config.py`: 配置管理，Neo4j图模板

### src/utils/
**工具模块**
- `utils.py`: 通用工具，包含LLM模型获取（含Brotli错误修复）
- `logger_config.py`: 日志配置，统一日志管理
- `data_process.py`: 数据处理工具

### src/prompts/
**提示词模块**
- `prompt.py`: 所有提示词模板

### src/data/
**数据模块**
- `data.py`: 数据处理逻辑

## 🔧 开发指南

### 添加新功能

1. **核心逻辑**: 在 `src/core/` 中添加新的模块
2. **工具函数**: 在 `src/utils/` 中添加工具函数
3. **测试用例**: 在 `test/unit/` 中添加单元测试
4. **文档更新**: 在 `docs/` 中更新相关文档

### 代码规范

- 所有代码必须包含详细的英文注释
- 所有方法必须添加日志记录
- 所有公共API必须有文档字符串
- 遵循 PEP 8 代码风格

## 📦 依赖说明

核心依赖：
- `gradio`: Web界面框架
- `langchain`: LLM应用开发框架
- `transformers`: HuggingFace模型库
- `py2neo`: Neo4j图数据库驱动

## 🐛 已知问题

### Brotli压缩错误
已集成修复方案，在 `src/utils/utils.py` 的 `get_llm_model()` 函数中实现禁用响应压缩。

## 📄 许可证

MIT License

## 👥 贡献

欢迎提交Pull Request和Issue。

---

**作者**: zjy
**更新时间**: 2024年
