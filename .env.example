# Java文档问答RAG系统 - 环境变量配置示例
# 复制此文件为 .env 并填入实际的配置值

# ==============================================
# LLM模型配置
# ==============================================
# DeepSeek API密钥（必填）
OPENAI_API_KEY=your_api_key_here

# API基础地址（可选，默认值如下）
OPENAI_API_BASE=https://api.deepseek.com/v1

# ==============================================
# 嵌入模型配置
# ==============================================
# 嵌入模型路径（必填）
MODEL_PATH=C:/02-study/model/embedding/bge-base

# 嵌入模型名称（可选）
EMBEDDING_MODEL_NAME=bge-base

# ==============================================
# Neo4j图数据库配置
# ==============================================
# Neo4j数据库URI（可选，默认值如下）
NEO4J_URI=neo4j://127.0.0.1:7687

# Neo4j用户名（可选，默认值如下）
NEO4J_USERNAME=neo4j

# Neo4j密码（必填）
NEO4J_PASSWORD=your_neo4j_password

# ==============================================
# 数据库路径配置
# ==============================================
# 向量数据库路径（可选，默认值如下）
VECTOR_DB_PATH=resources/data/db

# 输入数据路径（可选，默认值如下）
INPUT_DATA_PATH=resources/data/inputs

# ==============================================
# 性能配置
# ==============================================
# 缓存大小（可选，默认值如下）
CACHE_SIZE=1000

# 最大查询结果数（可选，默认值如下）
MAX_QUERY_RESULTS=3

# ==============================================
# 日志配置
# ==============================================
# 日志级别（可选：DEBUG, INFO, WARNING, ERROR）
LOG_LEVEL=INFO

# ==============================================
# 安全配置
# ==============================================
# 允许的域名列表（用于网络请求验证）
ALLOWED_DOMAINS=so.com,deepseek.com

# 网络请求超时时间（秒）
REQUEST_TIMEOUT=30

# ==============================================
# 使用说明
# ==============================================
# 1. 将此文件复制为 .env
# 2. 填入实际的配置值
# 3. 确保API密钥和数据库密码等敏感信息不要提交到版本控制系统
# 4. 在生产环境中使用更强的密码和密钥
#
# 注意事项：
# - OPENAI_API_KEY 和 NEO4J_PASSWORD 是必填项
# - MODEL_PATH 指定的路径必须存在
# - Neo4j服务必须正在运行
# - 建议定期轮换API密钥和数据库密码